<!DOCTYPE html>
<html lang="en">

<%- include("partials/head.ejs") %>

    <body>
        <style>
            body {
                background-color: lightcoral;
                color: aqua;
            }
            h1 {
                text-align: center;
                font-size: 75px;
                font-weight: bold;
            }
            .container{
                display: flex;
                justify-content: space-around;
                flex-flow: row wrap;
            }
            .stats{
                border: solid 2px blue;
            }
            .damages{
                border: solid 2px blue;
            }
            li {
                list-style: none;
            }
        </style>
        <h1>
            <%= pokeIndex.name %>
        </h1>

        <div class="container">
            <div>
            <img src="<%= pokeIndex.img %>">
            <h3> Type: <%= pokeIndex.type %>
            </h3>
            </div>
                <div class="stats">
                <h4>Stats</h4>
                <ul>
                    <% for(let element in pokeIndex.stats) { %>
                        <li>
                            <%= element %> : <%= pokeIndex.stats[element] %>
                        </li>
                        <% } %>
                </ul>
                </div>
                <div class="damages">
                <h4>Damages</h4>
                <% for(let element in pokeIndex.damages) { %>
                    <li>
                        <%= element %> : <%= pokeIndex.damages[element] %>
                    </li>
                    <% } %>
                    </div>

                </div>
    </body>

</html>